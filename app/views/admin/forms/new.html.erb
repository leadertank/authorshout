<section class="admin-shell">
  <div class="cms-header-row">
    <div>
      <h1>New Form</h1>
      <p>Create a flexible public form with optional one-time or recurring payment.</p>
    </div>
  </div>

  <%= render "form", form: @form %>
</section>